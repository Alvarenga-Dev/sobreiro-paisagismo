# Sobreiro Paisagismo

Projeto web configurado com React.js e Next.js usando a estrutura `app/`.

## Scripts

```bash
npm run dev
npm run build
npm run start
npm run lint
```

## Desenvolvimento

Instale as dependências com `npm install` e execute `npm run dev` para abrir o ambiente local.
